# TODO

Suikoを実行して見つかった未解決の問題と、診断品質を上げるための評価課題を記録する。完了した項目は残さない。

機能数を揃えること自体は目的にしない。Suikoの単一バイナリ、オフライン実行、決定的な検出という境界を保ちながら、実際の診断品質またはAgent Skillの利用品質が下がる差だけを扱う。

## 比較スナップショット

2026-08-18に、Natural Japaneseの`0f1cc1c5a4e2aa7590598c88a15c213a60d9545a`と、書籍・連載29原稿を`essay`として比較した。Suikoはプロジェクト設定を無効化し、両者で読解負荷も有効にした。

注: この表はLindera/IPADIC時点の測定である。同日中に形態素解析器をsudachi.rs（SudachiDict core 20260723固定）へ切り替えたため、29原稿比較はSudachi版で再実行するまで古い。回帰fixture上の切り替え差分は`low_specificity`の1件のみ（eval/calibration.md）。

| 観点 | Natural Japanese | Suiko | 一致 |
|---|---:|---:|---:|
| 通常finding | 1,001件 | 974件 | カテゴリ別プロファイル 1/29原稿 |
| 読解負荷 | 129件 | 137件 | カテゴリ別プロファイル 14/29原稿 |
| `outline`の抽出項目 | - | - | 29/29原稿 |
| `outline`の見出し統計 | - | - | 8/29原稿 |
| `terms`の候補数 | 1,049件 | 1,031件 | 候補集合 6/29原稿 |

findingや候補の件数が多いことを、精度が高いこととはみなさない。差分は正解ラベル付き評価集合で誤検知と見落としに分ける。

## 競合比較から採用した判断

2026-08-18時点の一次資料と実行結果から、競合の機能を次のように分類した。機能名だけを移植せず、Suikoの診断品質または利用経路を改善するものだけを未解決TODOへ接続する。

| 比較対象 | Suikoより強い能力 | Suikoでの扱い | 対応TODO |
|---|---|---|---|
| Natural Japanese | SudachiPy基準の形態素検出、広い用語候補 | 実コーパスで差を測り、Linderaで精度が上がる差は残す | 検出器の再校正、`terms`精度 |
| textlint | 列・範囲、formatter、修正候補、MCP、processorとルール資産 | 位置情報と安全な提案を先に採用し、汎用プラグインとMCPは利用価値を測ってから判断する | finding位置、安全なdiff、連携経路、拡張性 |
| AI writing preset | 短い文書でも発火する局所的なAI記述パターン | 文書全体の密度検出とは別カテゴリで評価する | 局所AIパターン |
| prh | 宣言的な表記辞書、差分表示、明示的な置換 | 一般的な置換器は再実装せず、複数文書の用語監査と安全な提案に限定して取り込む | `terms`精度、安全なdiff |
| RedPen | 複数マークアップ、注釈による抑制、拡張、エディタ連携 | 実利用がある形式と連携だけを候補にする | 連携経路、対応形式 |
| Vale | 単一バイナリ、YAMLスタイル、vocabulary、複数形式、エディタ/CI連携 | 配布形態は参考にし、任意ルール実行より宣言的な語彙を優先する | `terms`精度、連携経路、拡張性 |

Suikoが維持する差別化は、埋め込み辞書を含む単一バイナリ、実行時ダウンロードなし、同じ入力に同じfindingを返すこと、日本語の自然さと構造へ対象を絞ることである。競合に存在するという理由だけで、自動修正、重量モデル、任意コード実行型プラグインを既定機能へ入れない。

### 追加調査した競合

次のサービスとツールは公開された公式仕様を比較した。アカウント、契約、原稿の外部送信が必要なものは実行していないため、検出精度の優劣は未評価である。

| 比較対象 | 確認できた能力 | Suikoへの示唆 |
|---|---|---|
| [Shodo](https://developer.shodo.ink/docs/getting-started/try-shodo-api) | 日本語AI校正API、Python CLI、同期/非同期クライアント、行・文字位置を持つ結果 | 位置情報とAgent連携の参考になる。クラウド認証をSuiko本体へ持ち込まず、比較する場合も合成fixtureだけを送る |
| [Typoless](https://typoless.asahi.com/feature) | AIと大規模ルール辞書、カスタム辞書、良文支援、D&I/炎上リスク、Office・PDF・API連携 | 一般校正、組織辞書、リスク表現、バイナリ文書連携はSuikoより広い。機密文書を外へ出さないSuikoの境界と分けて評価する |
| [Just Right!7 Pro](https://www.justsystems.com/jp/products/justright/) | 誤字・誤用、不適切表現、表記揺れ、用語基準、訂正候補、公用文ルール | 一般校正の網羅性は再実装せず、Suikoが自然さへ集中する判断を実コーパスで確認する |
| [文賢](https://help.bun-ken.net/manual/) | 校正と推敲の分離、推奨語、コメント、補足、管理メモを持つ共有辞書、漢字使用率 | `.suiko.toml`の許可理由を、将来の用語監査でも失わない。単純な置換対だけで辞書を設計しない |
| [enno](https://enno.jp/) | 誤字脱字とタイポへ対象を絞った安定したパターン検出、誤検出抑制 | 少数の高精度ルールを優先する設計の比較対象にする。一般タイポ検出を無制限に追加しない |
| [jReadability](https://jreadability.net/sys/q_and_a?lang=ja) | コーパス由来の式による6段階の日本語難易度、語彙情報、500〜1,000字程度を前提とした評価 | 自然度、読解負荷、対象読者にとっての難易度を同じスコアへ混ぜない。難易度を出すなら文書長と対象読者の適用範囲を示す |
| [LanguageTool](https://dev.languagetool.org/languages) | 多言語の文法・スタイルAPIと宣言的ルール。日本語にも公開ルールがある | 日本語自然さの直接比較より、ルールID、置換候補、HTTP/エディタ連携の参考にする |
| Harper | Rust製、オフライン、Wasm、LSP、エディタ統合を持つ文法チェッカー | 現在は英語専用なので検出精度の競合ではない。Rustライブラリ分離、低遅延、組み込み配布の設計比較に限る |
| [yoin](https://github.com/agatan/yoin) | 純Rustの形態素解析器。mecab-ipadicをbzip2圧縮9.7MBでcrateに同梱しbuild.rsで展開・埋め込み | 2017年から未メンテで解析器としては不採用。辞書のcrate同梱手法も、SudachiDict coreはzipで69MBありcrates.ioの10MB制限に収まらないため転用できない（分割crate化はレジストリ濫用になるため行わない）。現行のビルド時SHA-256固定取得を維持する |
| [notmecab-rs](https://github.com/wareya/notmecab-rs) | MeCab辞書を実行時に読み込む純Rustトークナイザ。最小コスト保証、接続行列キャッシュ、コスト内省 | 2020年から未メンテで解析器としては不採用。実行時辞書読み込みは単一バイナリの境界に反する。トークン化コストを曖昧性シグナルとして使う案は、執筆者ジョブへの効果が未実証のため採用しない（コーパス整備後の研究候補に留める）。性能技法はwarm 0.34秒/14ファイルの現状に不要 |

商用サービスの非公開辞書件数や宣伝上の精度を、Suikoの目標値には使わない。比較実験する場合は、利用規約で許可された合成fixtureを使い、入力データ、プラン、実行日、設定を記録する。

## v0.3.0: 経験則の閾値と語彙を実測校正へ置き換える

2026-08-18に、Natural Japanese・類似ツール・関連研究を調査してv0.3.0の範囲を決めた。テーマは「実装済み機能の閾値・語彙・severityを、再現可能なコーパス実測で裏付けられたものへ置き換える」で、v0.2.0で残した「実コーパス待ち」の課題群を解消する。

### 調査結果

- Natural Japanese（coji/natural-japanese、MIT）は比較時点`0f1cc1c`が最新で、検出器の差分はない。ただし`corpus/`に**人間105ソースのsources.json**（URL・genre・license注記付き、本文非コミット）、AIコーパス生成runner、**人間FP率5%未満の制約下で検出率を最大化するcalibrate.py**があり、103/81文書測定の再現手順が実在する。genreに`slide`（12ソース）を持つ点だけSuikoと異なる
- textlint-rule-preset-ai-writingの5ルールのうち4つ（リスト装飾、誇張、コロン継続、強調密度）はv0.2.0で対応済み。残る差分は`ai-tech-writing-guideline`の冗長表現指摘（「〜を行う」等）
- Kobak et al. 2025（Science Advances）の**excess vocabulary法**: 人間コーパスとAIコーパスの語句頻度比から語彙マーカーを選ぶ。禁止語・誇張語彙を経験則でなくデータで出し入れする方法論として転用できる
- 日本語のAI文特徴の報告（広島大2025卒論ほか）: 文頭接続詞の過剰（文ごとに接続詞）、文体の完全な一貫性（文頭接続詞の過剰は2026-08-18の実測で本コーパスの人間/AIを分離せず、finding化せず観測値に留めた。eval/calibration.md）
- perplexity・embedding・分類器はNatural Japaneseのexperimentsでも検証済みで、モデル依存・実行時重量の点でSuikoの境界に反するため引き続き採用しない

### 採用（全て実施済み）

v0.3.0の採用項目は2026-08-19までに完了した。コーパス取得基盤（sources.toml 93件、fetch 81/81成功、AI生成runner、青空文庫12件）、`suiko-eval calibrate`（Wilson上限制約・dev限定・`--external`で外部取得文書をlock照合のうえ使用、欠落は明示skip）、`suiko-eval vocab`（語句別実測と追加候補の提示）、および現代人間75文書での再校正とholdoutの一度きり評価。判断の全記録はeval/calibration.md、判定ルールの事前登録はeval/annotation-guide.mdにある。

実測に基づく主な変更: `low_lexical_diversity_ttr`（長さ交絡、fpr 0.613）・`repeated_sentence_lead`（同、fpr 0.613）・`low_lexical_diversity_mtld`（全閾値でAI検出0）をEXPERIMENTALへ降格、「のではないでしょうか」を弱シグナル化、`low_specificity` -0.10緩和候補を棄却（fpr 0.133）。

v0.3.0後に残る校正課題:

- 確認候補系カテゴリ（`forbidden_phrase` dev 0.493/holdout 0.700、`translationese_morph` 0.573、`redundant_light_verb` 0.453/0.550）のヒット単位ラベリング。「発火=誤検知」ではなく「その指摘に従うと文が良くなるか」で適合率を測り、severity・語彙の見直しはその後にする
- 降格した3検出器の長さ正規化での再設計（TTRはMTLD系の長さ頑健な代替、文頭反復は文数比か文書長ゲート）
- 語彙の追加・削除はAI側10文書以上で再開（現在4文書。複数モデル・複数時期への拡張が先）
- `low_burstiness`の文書種別（記事/README/手順書/用語集）ごとの適用範囲測定（記事では dev 0.293/holdout 0.300 と許容範囲）

### 悪文（日本語言語学の古典的な読みにくさ）の検出範囲

2026-08-18に、岩淵悦太郎編著『悪文』（章立て: 悪文のいろいろ/構想と段落/文の切りつなぎ/文の途中での切り方/文の筋を通す/修飾の仕方/言葉を選ぶ/敬語の使い方）、本多勝一『日本語の作文技術』（修飾語の語順4原則と読点2原則）、千早耿一郎『悪文の構造』を調査した。文献の主要パターンは`skills/suiko/references/readability-antipatterns.md`のカタログA〜Jに収載済みで、機械検出の採否は次のとおり（実測はeval/calibration.md）。

- 実装済み（読解負荷レーン）: 一文の長さ（B1）、二重否定（A1）、「の」3連（C2）、漢字連続（C1）、埋没した列挙（F1）、読点ゼロの60字超（B4の下位事例、2026-08-18採用の`no_comma_sentence`）
- 測定して不採用: 「〜が、〜が、」連鎖（0/4,899文で過去の削除判断を再確認）、連用中止3連（自然な並列列挙に発火し適合率不足）、こそあど密集（全コーパスで1件のみ）
- 構文解析が必要で対象外（目視・カタログ担当）: ねじれ文（B2）、係り受けの曖昧さ（B5、`nested_attributive`廃止の経緯）、修飾語の語順（B3）、読点の位置・過多（B4のNO-GO維持）
- 一般校正の境界の外: 敬語の誤り（keigo.md）、慣用句の誤用、重言などの辞書型冗長表現

残る候補は、受動態の乱用（C3）とサ変名詞化（C2の未実装側）を`suiko-eval calibrate`の人間FP率制約下で測定することだけとする。

### 不採用

- perplexity・embedding・分類器・著者判定スコア: 単一バイナリ・決定性・「出自≠品質」の境界に反する
- です・ます完全一貫性の検出: 推敲された人間の文章にも当てはまり誤検知が多い。文体混在の指摘は一般校正（textlint等）の担当と衝突する
- textlintの強調反復ルール相当の追加: `high_bold_density`と概ね重複。実需要が出るまで見送る
- 用語辞書スキーマ: `terms --audit`の利用実績待ちを維持し、v0.3.0に入れない

## P1: sudachi.rs版の検出器を実コーパスで再校正する

### 観測結果

2026-08-18に、形態素解析器をLindera/IPADICからsudachi.rs（v0.6.11、SudachiDict core 20260723をSHA-256固定で埋め込み、Mode C）へ切り替えた。SudachiPy基準のNatural Japaneseと辞書系列が揃い、解析器差という変数が消えた。二つの回帰fixtureでの切り替え差分は`low_specificity`の1件（Mode Cの長単位化で1段落のスコアが閾値を上回った）だけで、他カテゴリは一致した。

| fixture | 通常 | `--experimental` |
|---|---:|---:|
| AI的な文書 | 24件（切替前25件） | 32件（切替前33件） |
| 自然な文書 | 0件 | 0件 |

書籍・連載29原稿のNatural Japanese比較はLindera時点の測定であり、Sudachi版で再実行するまで古い。`nominal_ending`と見出し統計の既知差も、Sudachiでの再測定が必要になった。

評価集合は人間14文書（+外部取得64 dev/17 holdout）・AI4文書になった。AI側にはClaude（claude-fable-5）生成の未修正文書3件（tech/essay/business、うちtechとessayは4,000字超）を出典・SHA-256付きで追加し、「4,000字以上のAI文書がなく検出率を比較できない」問題は解消した。`corpus.toml`の`[[sample]]`で正解ラベル付きサンプル57件（15カテゴリ）を管理し、`suiko-eval labeled`がカテゴリ別のdetectionとfprを出す。sweepは6ルール（文頭反復、TTR、MTLD、`low_specificity`、名詞止め、読解負荷の文長）へ広がった。

Sudachiでの実測では、「堕落論」のTTRは0.4107、AI tech文書は0.446で、人間文書のほうが低い。0.45では両方発火し、0.40では両方沈黙する。文書単位のTTRは、意図的反復とAI的な語彙平板を、この規模の評価集合ではどちらの向きにも分離できない。既定値0.45は変更せず、堕落論の発火を既知の不一致としてmanifestに記録した。各閾値が何を支え、何を支えないかはeval/calibration.mdにある。

### 期待する動作

- 移植前の件数一致ではなく、Suiko自身の誤検知率と検出率から閾値を決められる
- Sudachiの分かち書きに依存するカテゴリを、切り替え後の実測値で評価できる
- 検出器、閾値、ジャンル、文書長を変えたときの退行を、`suiko-eval`で再現可能に確認できる

### 設計上の確認事項

第三者のコーパス本文を無条件に複製しない。ライセンス、再配布可否、取得手順を分け、必要ならURLとハッシュだけを管理する。切り替え前後の完全一致も要求しない。形態素解析器の差が診断品質を改善するなら、その差は残す。辞書の版はbuild.rsのSHA-256で固定し、更新時はfetch-dictionary.shと同時に変えて全評価を再実行する。

### 完了条件

- 出典、利用条件、SHA-256を持つ人間文書を増やす（2026-08-19に完了: 青空文庫12件+外部取得81件。AI側の複数モデル・複数時期への拡張が残り）
- 書籍・連載29原稿のNatural Japanese比較をSudachi版で再実行し、残る差を誤検知と見落としへ分類する
- TTRと`low_specificity` -0.10の再検討は2026-08-19に完了（TTRは降格、-0.10は棄却。eval/calibration.md）

## P1: `terms`を用語監査として信頼できる精度へ上げる

### 観測結果

候補集合が一致するのは29原稿中6原稿だけである。Natural Japaneseだけが拾う候補は75件、Suikoだけが拾う候補は57件残る。たとえばSuikoは`応急処置`、`境界線`、`現実世界`を落とす一方、文脈によって`毎日`や`Social`を候補にする。解析器の件数一致ではなく、専門用語として確認する価値があるかを評価する必要がある。

また、Suikoの`terms`はファイルごとの候補一覧に留まる。prhの置換辞書やValeのvocabularyのように、プロジェクト全体の表記揺れ、推奨語、禁止表記を監査する能力はない。

### 期待する動作

- 候補抽出の適合率と再現率を、正解ラベル付きfixtureで判断できる
- 複数ファイルで同じ概念の表記揺れと初出説明を一覧化できる
- 自動置換を前提にせず、プロジェクトの正本用語と例外理由を確認材料として出せる
- 推奨語だけでなく、適用範囲、説明、例外理由、管理上のメモを保持できる

### 完了条件

- Natural Japaneseとの75件/57件の差をサンプリングして正解ラベルを付け、Sudachi側の抽出条件を再校正する（29原稿コーパスが必要）
- 用語辞書を追加する場合は宣言的なデータに限定し、推奨語、理由、対象パス、例外を表現できる最小スキーマを決める（`--audit`の利用実績を見てから設計する）
- CSVなどから取り込む場合は、正規化後の差分を表示してから保存し、未知フィールドや重複ルールを黙って捨てない
- 既定ではファイルを書き換えず、辞書の共有やimportも明示操作に限定する（`--audit`は読み取り専用で実装済み）

## P2: 自然度・読解負荷・読者別難易度を分離する

### 観測結果

Suikoは通常findingと`reading_load`を分けているが、「この文章はどの読者に難しいか」は評価しない。jReadabilityは、日本語学習者向けの基準コーパスから文章難易度を6段階で示し、平均文長、動詞・助詞の比率などを使う。一方で、500〜1,000字程度の文章を前提とし、1文だけの測定や形態素解析誤りによるずれを適用限界として明記している。

自然度スコア、読解負荷、文章難易度を一つの数値で表すと、「人間らしいが専門家向けで難しい文章」と「平易だが機械的な文章」を区別できなくなる。

### 期待する動作

- 自然さ、局所的な読解負荷、対象読者に対する難易度を別の概念として出力する
- 文書長、ジャンル、想定読者が評価範囲外なら、数値を出さず理由を返す
- 難しい文章を自動的に悪い文章と判定しない

### 完了条件

- 初学者、一般読者、技術者向けの正解ラベル付き日本語コーパスを用意する
- コーパスが揃った時点で、観測値の各特徴が判定へ与える影響を記録し、既存の`reading_load`との相関・追加価値・誤判定を比較してからスコア化を判断する

## P3: 一般校正との境界を比較fixtureで固定する

### 観測結果

Typoless、Just Right!、文賢、enno、LanguageToolは、誤字脱字、誤用、表記揺れ、用語基準、訂正候補など一般校正の範囲を広く扱う。SuikoはREADMEで一般校正の網羅を対象外としているが、どの問題を外部ツールへ任せ、どの問題を自然さの診断として扱うかを実行結果で固定していない。

境界が曖昧なままルールを追加すると、Suikoが小さな校正器の再実装になり、日本語の自然さに固有の検出を改善する時間が失われる。逆に、頻出する明白なタイポまで一切指ささない場合、単一バイナリだけを使う利用者の期待とずれる可能性がある。

### 期待する動作

- 自然さ、一般校正、組織固有ルールの担当範囲をfixtureと文書で説明できる
- Suikoで扱わない問題は、併用すべきツールまたは工程を案内できる
- 新しい一般校正ルールは、十分な頻度と高い適合率が確認できたものだけ採用する

### 完了条件

- 規約上実行可能な商用サービスで結果を比較する（OSS側はtextlint ja-technical-writing presetで2026-08-18に比較済み。6分類とも検出されず境界分類を裏付けた。`eval/boundary/README.md`）
- 実装対象を採用する場合は誤検知率と保守コストを記録し、競合の非公開辞書を推測で複製しない

## P3: ルール拡張と対応形式の優先順位を実利用から決める

### 観測結果

textlintはJavaScriptルールとprocessor、RedPenは複数マークアップと拡張、ValeはYAMLスタイルと複数形式を扱う。TypolessはWord、Excel、PowerPoint、PDF、ブラウザから利用できる。SuikoはMarkdownとプレーンテキスト、組み込みルール、`.suiko.toml`の無効化と理由付き許可に絞っている。

汎用プラグイン機構を追加すると配布と再現性の強みを薄める。一方、Re:VIEWやAsciiDoc、プロジェクト固有語彙の需要が実際にあるなら、現在の固定境界では不足する。

### 完了条件

- 実際の利用リポジトリから必要な形式とカスタムルール例を集める（実利用1件目: 技術書翻訳リポジトリのEN/JAコメントブロック形式は、生成される純日本語ファイルを対象にすれば現行機能で足りた。bilingual形式そのものへの対応は不要と確認）
- まず宣言的な語彙・禁止表現で満たせるか確認し、任意コード実行型プラグインは最後に検討する
- 新形式を追加する場合はfront matter、コード、リンク、表、コメントのマスクfixtureを形式ごとに追加する
- OfficeやPDFは本体で解析する前に、既存の抽出器から標準入力へ渡す方法で要求を満たせるか確認する
- 未導入の機能はREADMEの比較表で代替ツールを案内する

## P2: 実コーパスが必要な再校正の残り

実装とfixture校正は済んでいるが、閾値・語彙・区別条件の最終判断に実コーパスのラベルが必要なものをまとめる。2026-08-18の技術書翻訳リポジトリ（ja 14ファイル）での実測は反映済みで、次は203ファイルのblogsコーパスなど別ジャンルでの確認が対象になる。

- `antithesis_repetition`のseverity段階（比率0.02 / genre別critical閾値）を再校正する。長文でinfoに留まるケースは`tests/cli.rs`で固定済み
- `repeated_sentence_lead`の定型フィールド判定（コロン型とQ./A.型は実装済み）による見落としを、`glossary.md`を含む実文書で確認する
- `hype_expression`: 技術書翻訳の10件は全て原文の強調（groundbreaking/dramatically/explodes）の忠実訳で「維持」が正解だった。翻訳文書での扱い（`.suiko.toml`のallow運用か、ジャンル別抑制か）を決め、語彙追加は頻度と適合率が確認できたものだけにする
- `translationese`表層パターン（にとって重要、という点で、を持つこと、することによって）は、203ファイルで自然な用例にも発火した。自然用例のnegative fixtureと係り先での区別は未着手（形態素側の「は」型・使役型は2026-08-18に除外済み）
- suggestion allowlist: 「することが可能です」型は、実コーパスでの出現と安全性確認が取れてから追加する

## P2: `low_burstiness`の適用範囲と閾値を再校正する

### 観測結果

`blogs`配下のMarkdown 203ファイル中165ファイル、81.3%で`low_burstiness`が発火した。記事だけでなく、Skill、規則、README、用語集のように文体目的が異なる文書も同じ判定を受ける。発火率が高いため、修正すべき単調さと、説明文・定型文に自然な均一さを優先順位づけできない。2026-08-18の技術書翻訳リポジトリ（tech、ja 14ファイル）でも10ファイル（71%）で発火しており、書籍本文でも高止まりする。

### 期待する動作

- genreだけでなく文書長、構造化率、文書種別を考慮し、適用外なら理由を示してskipする
- 「多くの文書にある特徴」ではなく、人間が単調だと判断する文書を優先して示す
- 閾値と計算値が、修正前後でどの程度改善したかを比較できる

### 完了条件

- 記事、README、手順書、用語集、Skill指示書を分けて発火率と適合率を測る
- 現在の203ファイルを回帰集合として保存可能な形にし、81.3%という発火率の内訳を記録する
- `low_sentence_variance`など近いカテゴリとの重複と役割分担を決める

## P1: 人手評価と統計的不確実性を評価契約にする

### 観測結果

`eval/corpus.toml`は文書6件（人間2、AI 4）と、13カテゴリ29件の正解ラベル付きサンプルを持つ。現状の`suiko-eval`は発火率、detection、fprを点推定で出すが、評価者数、個別判断、評価者間一致、信頼区間を記録しない。silent sampleが1件だけのカテゴリで`fpr=0.000`になっても、母集団で誤検知率が0%だとは判断できない。

[van der Lee et al. (2019)](https://aclanthology.org/W19-8643/)は、生成文の人手評価について、複数評価者、評価者間一致と信頼区間、百分率一致、十分な標本数、提示順のランダム化を報告するよう勧めている。一方、[Amidei et al. (2018)](https://aclanthology.org/C18-1281/)は、自然言語の正当な多様性まで消して一致率を上げる危険を指摘している。Suikoでも、不一致を単なるラベルノイズとして多数決で捨てると、意図的な反復や文体差を「誤り」に固定するおそれがある。

また、[Berg-Kirkpatrick et al. (2012)](https://aclanthology.org/D12-1091/)は、NLP評価の有意性がテスト集合と実利用データのドメイン差によって予測力を失うことを示している。現在の少数fixtureで選んだ閾値を、技術記事、エッセイ、業務文書へそのまま一般化できない。

### 期待する動作

- 「この箇所を直すべきか」「候補どおり直すと自然になるか」「意味や著者の声を損なわないか」を別の評価項目にする
- 評価者ごとの判断と理由を残し、合意、不一致、判断不能を区別する
- detectionとfprに分母と区間推定を併記し、標本が少なすぎるカテゴリは率を性能値として扱わない
- 閾値を選ぶ開発集合と、最後に一度だけ評価するholdoutを分ける
- finding単位を独立標本とみなさず、文書または著者を再標本化単位にする

### 完了条件

- 各サンプルを2名以上が独立評価し、百分率一致と適切な一致度、信頼区間を報告する。不一致は理由付きで残し、機械的な多数決だけで正解にしない（手順・評価項目・判断不能条件・記録範囲・採用の事前条件は2026-08-18に`eval/annotation-guide.md`へ固定済み。実施には評価者が必要）
- genre、文書型、著者をまたがない層化でholdoutを実際に作る（`split = "dev" | "holdout"`の契約とsweepからの除外は実装済み。人間文書が増えるまでholdoutは空）

`suiko-eval`は2026-08-18から、全出力に評価集合の版（manifestのSHA-256）と再標本化単位を記録し、各率へWilson 95%区間を併記して分母5未満をlow_nとして性能値から外す。

## P1: 評価コーパスの役割とAI由来ラベルの境界を固定する

### 観測結果

正式なmanifestは人間2文書・AI 4文書だが、`skills/suiko/references/`には「人間103文書 + AI 81文書」で校正した数値が複数残る。その184文書の出典、選定条件、license、SHA-256、実行設定は現在の`eval/corpus.toml`から再現できない。再現不能な測定と、版管理された小規模評価が同じREADME・Skillから参照されている。

人間文書を増やす候補にも役割の違いがある。[BCCWJ1](https://clrd.ninjal.ac.jp/bccwj/)は1億430万語を複数ジャンルから無作為抽出した均衡コーパスだが、主な収録年代は1986〜2005年である。[BCCWJ2](https://www2.ninjal.ac.jp/BCCWJ2/index.html)は2006〜2025年を加える計画だが、2026-08-18時点の公開分は2006〜2010年の書籍約2,300万語で、現在のWeb文書や技術文書をまだ代表しない。[JCoLA](https://aclanthology.org/2024.lrec-main.828/)は10,020文の日本語容認度データを持つが、言語学文献から抽出した単文の二値判断であり、記事全体の自然さ、読解負荷、著者の声の正解集合ではない。

さらに、現行評価はhuman/AIを主ラベルにしてdetectionとfprを出す。[Sadasivan et al. (2023)](https://arxiv.org/abs/2303.11156)は、AIテキスト検出が再言い換えで大きく弱まり、人間文書のspoofingも起こりうることを示している。Suikoの目的は著者判定ではなく修正候補の提示なので、出自を品質ラベルの代用にしてはいけない。

### 期待する動作

- コーパスを「閾値調整」「未見holdout」「構文境界」「実利用stress」の役割に分け、同じサンプルを都合よく使い回さない
- human/AIという出自、文章品質、genre、文書型、文書長を別の軸として保持する
- BCCWJは自然な用例の分布確認、JCoLAは単文の統語的な境界確認に限定し、文書自然度の正解ラベルとして扱わない
- AI文書は検出率を競う対象ではなく、モデル、prompt、生成時期、修正履歴による偏りを調べるstress sliceとして扱う
- コーパス本文を再配布できない場合も、sample ID、抽出条件、取得版、変換手順、hashから評価を再現できる

### 完了条件

- BCCWJ1/BCCWJ2を使う場合は、再配布条件を確認し、本文を無断収録せず、利用したsample IDと抽出条件を保存する
- JCoLAはlicenseと適用可能なSuikoカテゴリを確認し、統語的容認度の回帰集合として追加価値がある場合だけ採用する
- 2020年代の技術記事、業務文書、ブログについて、再配布可能な人間文書を複数著者から集め、著者または同一媒体が開発集合とholdoutをまたがないよう分割する
- AI stress setを複数モデル・prompt・生成時期へ広げる場合も、原文、言い換え、人手修正版を別ラベルにし、著者判定精度をSuikoの成功条件にしない

2026-08-18に実施済み: 103/81文書の旧測定はmanifest・出典・実行設定が残っておらず再現不能と確認し、`skills/suiko/references/`の2箇所へ「履歴であり現在の根拠に使わない」と明記した。現行コーパスのData Statements（目的・母集団・選定・時代・genre・license・既知の偏り）と、コーパスの役割分離（dev/holdout/構文境界/実利用stress）、AI由来ラベルを品質の代用にしない方針は`eval/README.md`へ固定した。

## 実装順序

依存関係を無視して機能を並行追加しない。v0.3.0の採用項目は完了した。次を以下の順で進め、各段階でfixture、実コーパス、公開JSONの互換性を確認する。

1. 人手評価手順、holdout、信頼区間を固定し、点推定だけで閾値を選ばないようにする
2. 103/81文書の旧測定を再現可能にし、外部コーパスとAI stress setの役割を分ける
3. 出典・SHA-256付きの人間文書を各ジャンル・文書長へ増やし、TTRと`low_specificity`の閾値を再検討する
4. 書籍・連載29原稿のNatural Japanese比較をSudachi版で再実行し、残る差を誤検知と見落としへ分類する
5. 203ファイルの実コーパスで、`low_burstiness`、反復系severity、局所パターン、翻訳調表層の各再校正を行う
6. `terms`のNatural Japanese差分へ正解ラベルを付け、`--audit`の利用実績を見て用語辞書スキーマを決める
7. 読者別難易度は、初学者・一般・技術者向けの正解ラベル付きコーパスが用意できた時点で追加価値を評価する
8. 規約上実行可能な商用サービスとの境界比較を行う
9. 対応形式、汎用拡張、MCP/LSPは、前段で不足が残ったものだけ採否を決める（連携3経路の実測: 14ファイルをwarm 0.34秒で`--format github`/`--format sarif`、標準入力+JSONは1章0.05秒。現時点でMCP/LSPが必要になる不足は観測されていない）

各項目は「競合と同じ件数になった」では完了としない。正解ラベル付き評価で診断品質が改善し、通常バイナリのサイズ、起動時間、決定性を許容範囲に保てたことを完了条件とする。
